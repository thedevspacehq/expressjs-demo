extends ../layout.pug

block meta
    title Update Post

block content
    form(action=`/posts/edit/${post.id}`, method="put") 
        input(type="text", name="title", value=`${post.title}`)
        textarea(name="content", cols="30", rows="10") #{post.content}
        input(type="submit", value="Submit")